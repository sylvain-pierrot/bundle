[package]
name = "bundle-cbor"
version = "0.1.0"
edition = "2024"

[features]
default = ["std"]
std = ["bundle-io/std"]

[dependencies]
bundle-io = { path = "../io", default-features = false }
thiserror = { version = "2.0.18", default-features = false }
