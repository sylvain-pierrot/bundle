[package]
name = "bundle-io"
version = "0.1.0"
edition = "2024"

[features]
default = ["std"]
std = []

[dependencies]
embedded-io = { version = "0.7.1", default-features = false }
thiserror = { version = "2.0.18", default-features = false }
