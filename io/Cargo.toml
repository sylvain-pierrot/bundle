[package]
name = "aqueduct-io"
version = "0.1.0"
edition = "2024"

[features]
default = ["std"]
std = []

[dependencies]
embedded-io = { version = "0.6", default-features = false }
thiserror = { version = "2.0.18", default-features = false }
